dstart = Date.parse("#{start_year+1}-02-13")

dc.set_summary_rule('卒論発表', :no_makeup)
dc.set_interval({:n => 365, :range => -7..7}, 1)

dc.add_param(:holiday, {:flag => false}, 5)
dc.add_param(:monthweek, {:month => 2, :week => 2, :flag => true}, 1)
dc.add_param(:monthweek, {:month => 2, :week => 3, :flag => true}, 1)
dc.add_param(:other_events, {:n => 50, :seed => 999913}, 1)
dc.add_param(:order, {:date => Date.parse("#{start_year+1}-02-06"), :direction => :after}, 5) # 卒論締切
