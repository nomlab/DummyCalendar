dstart = Date.parse("#{start_year}-04-08")

dc.set_summary_rule('入学式', :no_makeup)
dc.set_interval({:n => 365, :range => -7..7}, 1)
dc.set_timing('bulk')

dc.add_param(:date, {:month => 4, :day => 8, :flag => true}, 1)
