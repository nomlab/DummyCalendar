dstart = Date.parse("#{start_year+1}-02-25")

dc.set_summary_rule('大学前期入試', :no_makeup)
dc.set_interval({:n => 365, :range => -7..7}, 1)

dc.add_param(:date, {:month => 2, :day => 25, :flag => true}, 1)
